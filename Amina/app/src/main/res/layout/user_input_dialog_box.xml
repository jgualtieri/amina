<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/custom_dialog_layout_design_user_input"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="0dp">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/prompt"
        android:id="@+id/pinsImage"
        android:adjustViewBounds="true"
        android:cropToPadding="false"
        />

    <TextView
        android:id="@+id/dialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Help us help you"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:layout_below="@id/pinsImage"
        android:padding="16dp"
        android:paddingBottom="0dp"/>

    <TextView
        android:id="@+id/dialogText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Amina relies on feedback from our users to
provide current information that keeps
everyone safe.\n\nHelp us by marking your surroundings to
keep the community updated."
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:padding="16dp"
        android:paddingTop="0dp"/>

</LinearLayout>